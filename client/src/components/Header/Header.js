import React from 'react';
import styles from './Header.css';
import banner from '../../../assets/images/banner.png';

const Header = () => (
  <header>
    <div className={styles.title}>Github searching user</div>
  </header>
);
export default Header;
